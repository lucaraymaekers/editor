# Now

Debug:
 Show debug borders from parent to all children
 Stack for adding debug elements.
 Stack for adding flags.

Input should be computed in post order 
- Lister

For scrolling, when pressing the scrollbar and not the thumb I would like linear exp 0%-100% scrolling for fast scrolling.

For input and output devices:
Add icon for keyboard
Add icon for midi device
Add icon for synth

Make instrument per voice

Redo panels.

rjf jank
pushbuffer for rendering
editor demo: use showkey
demo debuggin features
Memory alignment struct

Make button around checkbox also toggle the checkbox.

Pop-pup floating box.

Text with 0 padding will be clipped, fix this.


The center vertically code is wrong. 

UI rects have floating point because of ParentPct , so round when doing that multiplication
Also make it so that the drawable window part is 1600x900 excluding the window border

SDF smoothness should only happen on the corners.
This way the straight lines will have the crisp color.

- Better buttons
  x Checkbox with differential (linear exp smoothing)
  - Better rounded corners (SDF)
  x Fix bug of border being too small

- Proper panel resizing

x Commands
  - Undo?

# Next
- Better scrolling
  - Autoscroll when playing
  - Scroll to End/Beginning
- Serialization
- Sheet music show more than one octave
  - Raise an octave for selection

# Future 
- File picker

- Sheet music format

- Elongate & Shrink notes to their nearest musical length
  - When unsure, multiple possibilites make the user decide.
  - Snap timestamp to be on beat

- Pattern match a selection a notes to make them uniform 
- Expand selection based on pattern.

- Metronome & sound output
- Stretch/Shrink BPM 
- Convert TimeSig?

- Multiple voices
  - Add a voice to a selection

- Take selection
  - Align only first note.

- Trim the recordstart to the first note played.

- Visualize BPM as you adjust it.
  (where the bars would go)

- Harmony visualizer/picker 
  // Show a possible next chord progression (based on previous chords from the selected chord)

- Command tree with undo&redo.
  - Read up on StarfreakClone article

- Lists of patterns you can choose from
  e.g., for accompaniments. arpeggio's etc.

# UI
- Show only selected notes.

- UI slider for things settings
- UI checkbox for buttons like "record", "play" etc. ?

- Save / Load records
  - UI lister to select one
- UI lister for instruments
- UI lister for commands

- UI lister logic
  - SizeParent from ChildrenSum
  - UI_BoxFlag_FillFreeWidth, UI_BoxFlag_FillFreeHeight

- MIDI from input struct so that hot reload works.

# Other
- Get all note timestamps in record time. 
- Nickname for MIDI devices.

# Archived
x Platform layer MIDI API
  x Trim silence
x UI vertical list that has same width for all children 
x Snap BPM to bass notes 
x Sheet music lines 
x Scroll the roll and sheet with xdelta.
x sf2 and TSF
x multiple panes
x Notes to linked list
x In defense of linked lists
x Pedal
  x update trim
  x visual in pianoroll 
x Sound on Windows
x TSF improvements
  x Have a nil tsf value / handle tsf 0
x Voices & TSF channels.
x Trim end of pedal correctly.
x Delete notes
x Pedal/sustain per voice
x Merge different duplicate device names into one?
x Debug stepping slider 
