# In defense of linked lists: https://www.dgtlgrove.com/p/in-defense-of-linked-lists
To mitigate performance degradation when we are memory bound you can point nodes to chunks of data.
An arena backing a linked list can back other types as well.
Pointers remain stable.

# Differential equations
Easier to describe change than absolute amounts.  (e.g., population grow/shrink)

phase space is a nice way to think about possible states and if they are stable. 
Also by studying some new learnings might carry over. 
Some solutions have a "seeds of chaos" which mean that small inacurracies can have a huge impact.

Most of the time the solution is unattainable so it is skipped.
 
Physics: Highest derivative is the Second Order Derivative

PDE have multiple inputs (Partial vs. Ordinal)

Required:
Calculus Derivatives/Integrals
Linear Algebra


# Midi
Musical Instrument Digital Interface.
Sounds are synthetic.

Series of events with some timing information. 

Each MIDI event has information for a "channel".
Maximum 16.

Tracks are events ocurringing in parallel. Whose events can be mapped ot other channels.

Messages are 7 bits.
If MSB we got varying length encoding.

The last IDs are saved.  Based on the MSB being set you can  set all subsequent ID with the same ID.

NoteOn with velocity of 0 = NoteOff.

UTF8

# Resources
- https://fgiesen.wordpress.com/2010/12/14/ring-buffers-and-queues/
- Basic MIDI Input/Output Programming 
- https://www.ccarh.org/courses/253/lab/midiprog/
- MSDN MIDI funtions
- (youtube) javidx9 -  Programming MIDI: https://www.youtube.com/watchv=040BKtnDdg0


# Questions
Where does MIDI latency come from.
Actual memory bandwidth or processing power?
Are there other formats that you can use than MIDI for sending events to the keyboard.
Why do you even need a NoteOff :(