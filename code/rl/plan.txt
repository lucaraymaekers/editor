# Misc
- bounds check on arrays

- Better profiler. (with UI)
  - graph(s)
  - computerenhance
  - Add profile toggle for app that doesn't necessarily inherit from platform (collapsing leaves from tree would be better)
- Cmdline hash parsing 
- tick() function?

# UI
- Treeview
- Hot && Active set on right click as well
- bug: Some borders of the buttons are not exactly 1px like they should be.

- Assert for adding the exact same box twice (?) / Collisions
- Implement properties from https://www.dgtlgrove.com/p/ui-part-3-the-widget-building-language
- inset elements
- ui: windows

# Renderer
- Speed up atlas creation.
  - Glyph cache for faster atlas creating

# Platorm
Misc:
- Make input be linked list of events
  - can easily be eaten.
- Replay improvements
  - More UI for operations  
  - Forward/Backward 1 frame
  x Multiple slots
  x Save/Load replay to/from file
  - Select a starting state 
  - Overwrite a recording buffer partially.
    - Idea: Always load/to Save.Buffer so that we have enough space to eventually get it out.
  - Track usage of permanentarena to write less data.  But in the end we might end up being using all that memory or more so does it really matter?
- Merge editor and muze
- keyboard input (pressing & holding, vs text)
  - easier query of keys & modifiers.
  - to have key binds. (WasPressed(Control, 'c')
- sleep threads when idle
- custom borders and titlebar

- Build as one executable without dll or shader code as text.

Logging improvements:
- Attach callstack, frame index, time to errors, (maybe even system tag) 
- Multiple levels
- Visual console to view logs

Linux:
- release clipboard when exiting
- ignore caps lock on key binds
- investigate startup time.
  - choosing visual config ~40ms
  - atlas = 90ms

# Done
x platform: Specify window name with P_Init
x ui: DisplayString should be only string, and UI_Label should be what I need for displaying text fields.
x Intrinsics
x add %S to stb_sprintf
